# Municipal Staduim Control

## Execução

Ao clonar o repositório, no diretório `/municipal-stadium-control` execute:

```
$ npm install
```

Agora, com as dependẽncias instaladas, você pode executar o projeto com:

```
$ docker-compose up
```